import './styles.css';
import type { ComponentProps } from './types';
import Component from './Component';

interface Config {
  enabled: boolean;
}

const config: Config = {
  enabled: true
};

export default config;